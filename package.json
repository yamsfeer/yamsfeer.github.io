{
  "name": "blog-pages",
  "version": "1.0.0",
  "private": true,
  "description": "Blog deployment repository - builds markdown posts into static HTML for GitHub Pages",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  }
}
